# {{PLUGIN_NAME}} - 插件开发项目

## 项目信息

| 属性 | 值 |
|------|-----|
| 插件ID | `{{PLUGIN_ID}}` |
| 名称 | {{PLUGIN_NAME}} |
| 版本 | {{PLUGIN_VERSION_NAME}} ({{PLUGIN_VERSION}}) |
| 作者 | {{PLUGIN_AUTHOR}} |
| UI类型 | {{UI_TYPE}} |
| 描述 | {{PLUGIN_DESCRIPTION}} |

---

## 目录结构

```
{{PLUGIN_NAME}}/
├── plugin.json          # 插件配置文件（不可改名）
├── icon.png             # 插件图标（可选，128x128 PNG）
{{DIRECTORY_TREE}}
```

{{DEV_GUIDE}}

---

## 编译打包

{{BUILD_STEPS}}

---

## 打包为 TPK

将以下文件打包为 ZIP，重命名为 `.tpk`：

```
{{PACKAGE_FILES}}
```

---

## 导入运行

1. 打开 UIN Tool App
2. 点击底部「管理」
3. 点击「导入插件」
4. 选择生成的 .tpk 文件
5. 在「工具」中运行插件

---

## 调试技巧

在 UIN Tool 的「管理」->「运行日志」中查看插件加载日志、错误和异常。

在代码中输出日志：

```java
// Java 中
com.UIN.Tool.utils.LogUtils.i("{{PLUGIN_ID}}", "调试信息");
```

```javascript
// Web 插件 JS 中
UINPlugin.callHost('log', '调试信息');
```

---

## 常见问题

Q: 插件导入后不显示？

· 检查 plugin.json 格式是否正确
· 检查打包内容是否完整（见「打包为 TPK」）
· 查看运行日志中的错误信息

Q: 后端无法启动？

· 确认「后端启动命令」已填写（默认 `sh scripts/start.sh`）
· 确认运行环境已安装所需依赖（Termux 用 pkg，proot 容器用 apk）
· 查看运行日志中的启动输出

---

## 更新日志

v{{PLUGIN_VERSION_NAME}} ({{PLUGIN_VERSION}})

· 初始版本

---

## 许可证

本项目采用 MIT 许可证。

---

## 联系方式

· 作者：{{PLUGIN_AUTHOR}}
· 插件ID：{{PLUGIN_ID}}