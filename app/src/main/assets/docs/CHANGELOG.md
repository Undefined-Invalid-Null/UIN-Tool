# UIN Tool 更新日志

本文档记录了 UIN Tool 的所有重要版本更新和功能变化。

---

## 版本命名规则

- **主版本号**：重大架构变更或不兼容的 API 修改
- **次版本号**：新增功能，向下兼容
- **修订号**：问题修复，向下兼容

---

## [4.2.0] - 2026-07-24

### 🎉 重大更新：Termux 后端集成 + 插件开发增强

#### Termux 后端集成

- **插件后端支持**：Web 插件可直接启动 Termux 后端服务
  - 支持 Python、Node.js、PHP 语言后端
  - 支持二进制可执行文件作为后端
  - 后端服务自动启动，用户无感知
  - 后端进程生命周期管理（插件关闭时自动停止）

- **后端通信协议**：
  - HTTP API 通信（无需 WebSocket）
  - 后端必须提供 `/health` 健康检查端点
  - 后端自动分配端口（默认 8000）

- **后端模板**：`python_template.tpk` 模板
  - 使用 Python 内置 `http.server`，无需安装 Flask
  - 兼容 Python 3.14+（移除 `cgi` 模块依赖）
  - 支持计算、记录、查询、系统命令等 API

#### 插件系统增强

- **插件说明功能**：
  - 插件可在 `plugin.json` 中声明 `notice` 字段
  - 首次打开插件时自动显示说明弹窗
  - 用户可选择「不再提示」或「稍后提醒」
  - 插件管理页面可查看完整说明

- **插件创建向导优化**：
  - 统一「创建插件」入口，弹窗选择前端类型
  - 支持原生 UI、纯 WebView、WebView + 后端三种模式
  - 后端选择：Python、Node.js、PHP、二进制文件
  - Web 插件默认生成空白 HTML/CSS/JS 文件
  - 二进制后端支持直接选择可执行文件

#### 开发工具增强

- **导出模板**：新增 `python_template.tpk` 模板
- **代码编辑器**：
  - 原生和 Web 插件都支持代码编辑器
  - 导入已有 Web 项目 (ZIP) 功能
- **向导步骤动态调整**：
  - 纯 WebView 插件：4 步（配置 → 图标 → 代码 → 打包）
  - Web + 后端插件：5 步（配置 → 图标 → 代码 → 资源 → 打包）
  - 原生插件：5 步（配置 → 图标 → 代码 → 资源 → 打包）
  - 二进制后端插件：4 步（配置 → 图标 → 选择文件 → 打包）

#### 后端管理

- **PluginBackendManager**：统一管理后端进程
  - 进程启动/停止/状态查询
  - 输出监控（stdout/stderr）
  - 健康检查（等待服务就绪）
  - 端口自动分配
  - 支持多语言解释器路径查找

---

### 🎨 UI 统一与优化

#### 弹窗统一

- 插件说明弹窗使用 Compose `AlertDialog`，与项目风格一致
- 白色背景，Material 3 按钮样式
- 移除所有 Emoji

#### 开发页面

- 按钮颜色统一（PrimaryButton / SecondaryButton）
- 弹窗背景白色
- 移除 Web 插件模板选择，仅保留导入和空白模板

---

### 🛠️ 技术改进

- **PluginInfo 扩展**：新增 `notice`、`backend`、`backendPort`、`backendEntry` 等字段
- **PluginManager 扩展**：新增后端管理方法和通知管理方法
- **PluginHostActivity 扩展**：支持显示插件说明弹窗
- **Python 路径查找**：优先使用 Termux 的 `python` 命令
- **编译优化**：Web 插件打包时自动处理二进制文件

---

## [4.1.0] - 2026-07-17

### 🎉 重大更新：代码编辑器升级 (Sora Editor)

#### 代码编辑器全面升级

- **Sora Editor 集成**：替换原有简陋编辑器，采用专业的 Sora Editor 引擎
  - 基于 `io.github.rosemoe:sora-editor` 0.24.4
  - 使用 `AndroidView` 在 Compose 中完美嵌入
  - 支持 30+ 种编程语言语法高亮

- **TextMate 语法高亮**：使用 `language-textmate` 模块提供专业级语法高亮
  - 支持 Java、Kotlin、Python、JavaScript、TypeScript
  - 支持 HTML、CSS、JSON、XML、Markdown
  - 支持 Shell、SQL、Go、Rust、PHP、Ruby、Swift
  - 支持 Dart、Lua、Scala、Perl、Haskell、Elixir
  - 支持 Erlang、Clojure、Groovy、Dockerfile、Makefile
  - 支持 INI、Properties、TOML、YAML 等 30+ 种语言

- **主题系统**：内置 28+ 种代码编辑器主题
  - 深色主题：dark-plus、dracula、one-dark-pro、material-theme 等
  - 浅色主题：vitesse-light、github-light、solarized-light 等
  - 一键切换主题，实时生效
  - 主题自动分类（深色/浅色）

- **编辑器功能增强**：
  - 行号显示
  - 代码折叠
  - 括号匹配高亮
  - 智能自动缩进
  - 撤销/重做历史
  - 搜索与替换
  - 文件树管理（侧边栏）
  - 文件类型图标（Material Icon）
  - 自定义配色方案

#### 技术实现

- **TextMate 初始化**：在 `UinApplication` 中全局初始化
  - 使用 `ThemeRegistry` 管理主题
  - 使用 `GrammarRegistry` 管理语法
  - 使用 `FileProviderRegistry` 管理 Assets 文件访问
  - 动态扫描 `assets/textmate/` 目录加载语法文件

- **编辑器集成**：
  - 使用 `AndroidView` 包装 `CodeEditor`
  - 通过 `ContentChangeEvent` 监听文本变化
  - 通过 `SelectionChangeEvent` 监听选择变化
  - 自动检测文件类型并设置对应语言

#### 编辑器主题切换

- 调色板图标一键打开主题选择器
- 深色/浅色主题分类展示
- 当前主题高亮显示
- 主题切换即时生效

---

### 🎨 UI 统一与优化

#### Toast 统一

- 移除所有 Emoji，使用 Material Icon 风格
  - 成功：[✓] 消息
  - 错误：[✗] 消息
  - 警告：[!] 消息
  - 信息：[i] 消息

#### 弹窗统一

- 统一使用 `UIComponents.ConfirmDialog`
- 统一使用 `UIComponents.InfoDialog`
- 统一使用 `UIComponents.LoadingDialog`
- 统一使用 淡入淡出 动画效果
- 移除所有 Emoji，替换为 Material Icon

#### 主题选择器统一

- 统一使用 `ThemeSelectionDialog`
- 28+ 主题完整列表
- 深色/浅色自动分类
- 当前主题高亮显示

---

### 🐛 问题修复

- 修复插件权限管理 UI
- 修复备份恢复功能
- 优化应用启动速度
- 修复退出应用取消按钮无效问题
- 移除 "已启用签名验证" 和 "当前已是最新版本" 前的 Emoji

---

### 📝 文档更新

- 更新 README.md 至 v4.1.0
- 更新开发文档，增加 Sora Editor 集成说明
- 更新使用帮助，增加代码编辑器使用指南

---

## [4.0.0] - 2026-07-14

### 🎉 重大重构：Kotlin + Jetpack Compose 全面升级

这是 UIN Tool 发展史上最重要的一次重构，整个应用从技术栈到架构都进行了全面升级。

---

### 🚀 技术栈全面升级

#### 编程语言迁移

- **Kotlin 全面迁移**：核心代码从 Java 迁移到 Kotlin
  - 134 个 Kotlin 文件，39 个 Java 文件
  - 利用 Kotlin 空安全、协程、扩展函数等特性
  - 代码量减少约 30%，可读性大幅提升
- **Java 遗留保留**：Termux 终端核心保留 Java 实现

#### UI 框架重构

- **Jetpack Compose**：从 XML 布局全面迁移到声明式 UI
  - 采用 Compose 2024.09.00 BOM
  - Material 3 设计语言
  - 响应式 UI 更新，无需手动管理 View 状态
- **ViewBinding 保留**：部分复杂界面保留 ViewBinding 支持
- **Compose 组件库**：40+ 可复用 Compose 组件

#### 架构重构

- **MVVM 架构**：引入 ViewModel + StateFlow 响应式状态管理
  - 9 个 ViewModel 覆盖所有主要页面
  - 单向数据流，状态可预测
- **Repository 模式**：数据层与业务层分离
  - `IConfigRepository`：配置管理接口
  - `IPluginRepository`：插件管理接口
  - `BackupRepository`：备份管理接口
- **依赖注入**：ServiceLocator 统一管理服务实例
  - 单例模式管理核心服务
  - 延迟初始化，优化启动性能
- **分层架构**：UI → ViewModel → Repository → DataSource
  - 清晰的职责划分
  - 便于单元测试和维护

---

### 🎨 UI 全面升级

#### Material 3 设计系统

- **全新视觉风格**：采用 Material 3 设计规范
  - 动态颜色系统
  - 圆角设计语言
  - 全新组件样式
- **深色模式完善**：完整的深色/浅色主题切换
  - 独立的深色模式颜色配置
  - 系统深色模式跟随
- **玻璃效果**：毛玻璃质感的 UI 组件
  - GlassCard、GlassButton、GlassDialog
  - 可开关的玻璃效果

#### 完整颜色选择器

- **RGB + Alpha 通道**：独立调节红绿蓝和透明度
  - 滑块式调节，实时反馈
  - 支持 0-255 精确控制
- **十六进制输入**：支持直接输入颜色值
  - #RRGGBB 格式
  - #AARRGGBB 格式（含透明度）
- **预设颜色**：16 种常用颜色快速选择
- **实时预览**：选择颜色时即时显示效果

#### 38+ 颜色配置项

| 分类 | 颜色项 |
|------|--------|
| 主色调 | 主题色、深色主题色、浅色主题色、强调色 |
| 辅助色 | 成功色、警告色、错误色、信息色 |
| 文本色 | 主文本色、副文本色、提示文本色、反色文本 |
| 背景色 | 背景色、表面色、变体表面色 |
| 边框色 | 分割线色、玻璃背景色、禁用色 |

#### 7 种圆角配置

- 小圆角、中圆角、大圆角、超大圆角
- 按钮圆角、卡片圆角、对话框圆角、输入框圆角
- 滑块实时调节，即时预览

#### 尺寸与字体配置

- **尺寸配置**：按钮高度/宽度/阴影、卡片阴影/内边距、间距、图标尺寸、进度条高度
- **字体配置**：标题/正文/辅助/章节标题字体大小
- **文字加粗开关**：全局文字粗细控制

#### 特效配置

- 玻璃效果开关
- 波纹效果开关

#### 配置管理

- **导入/导出**：JSON 格式导入导出 UI 配置
- **重置功能**：一键恢复默认配置
- **持久化存储**：配置自动保存

---

### 🖥️ 终端功能（基于 Termux）

#### 终端集成

- **内置 Termux 引擎**：完整集成 Termux 终端模拟器
  - 基于 Termux 核心代码改编
  - 适配 Compose UI 框架
- **完整 Linux 环境**：
  - APT 包管理器
  - bash、zsh 等主流 Shell
  - Python、Node.js、Ruby 等运行时
  - git、curl、wget 等开发工具

#### 终端管理

- **多会话支持**：同时运行多个终端会话
- **多窗口支持**：Android 7.0+ 多窗口/分屏
- **会话切换**：快速切换不同终端会话
- **会话重命名**：自定义会话名称

#### 终端配置

- **字体设置**：自定义终端字体
- **配色方案**：可自定义终端颜色
- **键盘快捷键**：硬件/软件键盘快捷键配置
- **额外按键**：可配置的额外功能按键

#### 文件系统

- **存储访问**：通过 `~/storage` 访问设备存储
  - `shared`：共享存储（内部存储根目录）
  - `downloads`、`dcim`、`pictures` 等
  - `external-*`：外部存储目录
  - `media-*`：媒体目录

---

### 🔌 插件系统增强

#### 插件权限系统

- **权限声明**：插件在 `plugin.json` 中声明所需权限
- **启动前检查**：打开插件前自动检查权限状态
- **分组请求**：按权限组请求，提升用户体验
  - 存储权限组、网络权限组、相机权限组等
- **权限状态可视化**：直观显示每个插件的权限状态
- **特殊权限处理**：悬浮窗、修改系统设置等特殊权限引导

#### 插件依赖管理

- **依赖声明**：插件可声明依赖的其他插件
- **依赖检查**：安装时自动检查依赖是否满足
- **缺失提示**：依赖缺失时给出明确提示

#### 插件生命周期增强

- **完整的生命周期管理**：创建、启动、暂停、恢复、销毁
- **多实例支持**：同一插件可同时运行多个实例
- **WebView 缓存管理**：自动管理 WebView 缓存

---

### 🛠️ 开发工具增强

#### 插件创建向导

- **原生插件向导**：
  - 自动生成 Kotlin 代码
  - 实现 PluginInterface 接口
  - 提供完整的生命周期方法
- **Web 插件向导**：
  - 完整模板（含 HTML/CSS/JS 示例）
  - 空白模板（基础框架）
  - 导入已有 Web 项目（ZIP 格式）
  - 自动生成 plugin.json

#### 编译系统

- **Java 源码编译**：集成 ECJ + D8
  - 当前为占位实现，原生插件编译功能暂时禁用
  - 原因：Android 环境缺少 tools.jar 支持
  - TODO：后续通过 Gradle 依赖或 Janino 替代
- **TPK 打包**：支持原生和 Web 插件打包
- **Web 插件直接打包**：无需编译，直接打包

---

### 📦 数据层重构

#### Repository 模式

- **IConfigRepository**：配置管理
  - 工作目录、视图模式、图标着色
  - 启动状态、更新相关、崩溃相关
  - 插件签名、插件权限
  - 镜像站、主题、UI 配置
- **IPluginRepository**：插件管理
  - 插件列表、安装/卸载
  - 搜索、分类、打开插件
  - 插件生命周期
- **BackupRepository**：备份管理
  - 创建/恢复/删除备份

#### 本地存储

- **PreferenceManager**：统一的 SharedPreferences 管理
  - 30+ 配置项
  - 类型安全的存取方法
- **FileManager**：文件操作封装
  - 复制、读写、删除、压缩、解压

#### 远程 API

- **GitHubApiService**：GitHub API 封装
  - 获取组织仓库列表
  - 获取最新 Release
- **MirrorManager**：镜像站管理
  - 镜像测试
  - URL 转换
  - 默认镜像列表

---

### 🎯 性能优化

#### 启动性能

- 冷启动时间优化 30%
- 延迟初始化非关键组件
- 优化 SplashActivity 加载逻辑

#### 运行时性能

- Compose 重组优化
- StateFlow 替代 LiveData，减少不必要的更新
- 协程替代 AsyncTask，减少线程开销

#### 内存优化

- WebView 缓存池管理
- 图片加载优化（采样缩放）
- 及时释放资源

---

### 🔒 安全性增强

- 插件签名验证（SHA-256）
- 插件权限独立管理
- WebView 安全配置（禁用 file 协议访问）
- 路径遍历防护

---

### 🐛 问题修复

#### 稳定性修复

- 修复 Activity 销毁后显示 Dialog 导致的 `BadTokenException`
- 修复 Fragment 销毁后更新 UI 导致的崩溃
- 修复 WebView 内存泄漏问题
- 修复插件加载过程中的类找不到异常
- 修复下载过程中 Activity 销毁导致的崩溃
- 修复 FileProvider 路径配置问题

#### UI 修复

- 修复 CheckBox 背景变色问题
- 修复图标着色失效问题
- 修复深色模式下颜色显示异常
- 修复小部件刷新失败问题
- 修复布局文件编译错误

---

### 📝 文档更新

- README.md 完全重写，反映 v4.0.0 变化
- CHANGELOG.md 更新至 v4.0.0
- 新增 API 文档
- 新增开发者指南

---

## 贡献者

感谢所有为 UIN Tool 做出贡献的开发者！

| 贡献者 | 角色 | 贡献内容 |
|--------|------|----------|
| UIN Team | 核心开发 | 架构设计、核心功能 |
| 一支电笔 | 功能开发 | 1x1 桌面小部件功能 |
| Termux 团队 | 上游项目 | 终端模拟器核心引擎 |

---

## 升级指南

### 从 v4.1.0 升级到 v4.2.0

v4.2.0 是 Termux 后端集成版本，升级前请注意：

1. **Termux 环境**：需要 Termux 终端环境已安装（插件向导会自动检测）
2. **Python 后端**：首次使用会自动安装 FastAPI 依赖（使用国内镜像源）
3. **插件说明**：旧插件可手动添加 `notice` 字段到 `plugin.json`
4. **兼容性**：完全向下兼容，所有旧功能保持不变

### 从 v4.0.0 升级到 v4.1.0

v4.1.0 是代码编辑器升级版本，升级前请注意：

1. **编辑器功能增强**：Sora Editor 提供专业级代码编辑体验
2. **语法高亮**：新增 30+ 种编程语言支持
3. **主题系统**：28+ 种编辑器主题可选
4. **兼容性**：完全向下兼容，所有旧功能保持不变

### 从 v3.x 升级到 v4.0.0

v4.0.0 是一次重大重构，升级前请注意：

1. **UI 完全重写**：基于 Compose 的全新 UI，交互方式有变化
2. **配置不兼容**：旧版 UI 配置需要重新设置
3. **插件兼容**：插件系统保持向后兼容
4. **终端功能增强**：基于 Termux 的完整终端体验

---

| 项目 | 信息 |
|------|------|
| 文档版本 | 4.2.0 |
| 最后更新 | 2026年7月24日 |
| 对应应用版本 | v4.2.0 (Build 12) |

---

© 2026 UIN Team. All Rights Reserved.